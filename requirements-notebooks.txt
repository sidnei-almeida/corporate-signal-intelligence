# Analysis stack for notebooks/ — kept separate from requirements.txt so the API
# deployment does not pull several gigabytes of scientific and GPU libraries.
#
#   pip install -r requirements-notebooks.txt
#
# PyTorch is installed from Intel's XPU index when an Arc GPU is present:
#   pip install torch --index-url https://download.pytorch.org/whl/xpu
# The autoencoder falls back to CPU automatically when no XPU device is enumerated.

pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0
statsmodels>=0.14.0
arch>=6.3.0
ruptures>=1.1.9
pyod>=2.0.0
shap>=0.45.0
xgboost>=2.0.0
lightgbm>=4.3.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.20.0
kaleido>=0.2.1
missingno>=0.5.2
joblib>=1.3.0
tqdm>=4.66.0
pyarrow>=15.0.0
requests>=2.31.0
python-dotenv>=1.0.0
jupyter-client>=8.6.0
ipykernel>=6.29.0
nbclient>=0.10.0
nbformat>=5.10.0
