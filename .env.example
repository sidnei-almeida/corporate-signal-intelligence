# Optional — executive briefings (POST /briefing)
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Optional — override default model/*.joblib
# MODEL_PATH=model/your_model.joblib

# Local dev port (Render sets PORT automatically)
PORT=8000
